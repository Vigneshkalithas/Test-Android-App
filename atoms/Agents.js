import { atom } from "recoil";

export const OpenBids = atom({
  key: "OpenBids",
  default: [],
});

export const Bids = atom({
  key: "Bids",
  default: [],
});

export const CompReport = atom({
  key: "CompReport",
  default: [],
});

export const AnalyticsData = atom({
  key: "AnalyticsData",
  default: [],
});
